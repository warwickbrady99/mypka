---
name: Pricing Strategy
key_element: work
parent_topic:
lifecycle: exploring              # exploring | promoted | dormant (default exploring)
promoted_to:                      # Key-Element slug - set ONLY when lifecycle: promoted
tags:
  - strategy
---

# Pricing Strategy

## What I think about here

The shape of this topic in your head. The recurring questions. Why it keeps coming back.

## Open questions

The things you do not yet have a clean answer to. When you decide to actually *solve* one, spin up a Project and list this Topic in that Project's `linked_topics` - the Topic stays `exploring` (it is feeding the Project, not retiring). This is different from graduating the whole Topic into a Key Element (see below).

## Graduation

If this Topic grows into a permanent domain of your life, it graduates into a Key Element: set `lifecycle: promoted` and `promoted_to: <key-element-slug>` (the new Key Element points back here via `promoted_from`). Example: **"French"** starts as a Topic; when it becomes a sustained, measurable pursuit it graduates to a Key Element, and a Goal ("reach B2 French") can then anchor there. Leave this section empty while the Topic is still `exploring`.

## Sources

Books, articles, people, conversations that shaped your thinking on this topic. Link to them.

- [[some-document-slug]]
- [[some-person-slug]]
