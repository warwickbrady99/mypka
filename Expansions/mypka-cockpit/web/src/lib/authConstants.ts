// authConstants.ts — values mirrored from server/auth.js so the client validates
// the same way the server does (kept tiny and dependency-free on purpose).
// If the server's MIN_PIN_LENGTH changes, update this to match.
export const MIN_PIN_LENGTH = 6;
