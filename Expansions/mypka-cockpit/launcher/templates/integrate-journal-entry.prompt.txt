I'm in my myPKA scaffold root. The myPKA Cockpit just launched you here so you
can take a quick journal entry I added by hand and properly fold it into my
knowledge base. Treat this whole message as the task.

THE RAW ENTRY:  {{ENTRY_PATH}}
MY ROOT:        {{ROOT}}

(Both paths are absolute and supplied by the cockpit. {{ENTRY_PATH}} is the
markdown file to work on; {{ROOT}} is the scaffold root you were launched in —
where AGENTS.md / CLAUDE.md / the team contracts and the PKM/ folder live.)

------------------------------------------------------------------------------
WHO SHOULD DO THIS
------------------------------------------------------------------------------
If this scaffold has a journaling / PKM-curation specialist — in the myPKA team
that is Penn, the Journal Writer & graph curator — route this task to them and
let them follow their own contract; the steps below are exactly the job they do.
If there is no such specialist, act as the PKM-curation assistant yourself and
perform the steps below directly. Either way, the procedure and the guarantees
are the same.

Before you start, read the scaffold's conventions so you match them instead of
inventing your own: the frontmatter schema (in myPKA: Team Knowledge/Guidelines/
GL-002-frontmatter-conventions.md) and the file-naming rules (GL-001). If this
isn't a myPKA scaffold, infer the conventions from a few existing notes and the
project's own docs, and follow those.

------------------------------------------------------------------------------
THE NON-NEGOTIABLE: PRESERVE THE ORIGINAL FIRST
------------------------------------------------------------------------------
Before you change a single word of the body, preserve my original text verbatim.
This is what powers the cockpit's "unfold original" — if you skip it, my words
are lost.

1. Read {{ENTRY_PATH}} in full.
2. If its frontmatter does NOT already have an `original_body` field, copy the
   ENTIRE current body (everything after the closing `---`) into a new
   `original_body` frontmatter field, as a YAML block scalar, byte-for-byte —
   keep my line breaks, my `#`, my `[[`, my typos, all of it. Do not clean,
   translate, or summarize the preserved copy.
3. If `original_body` is ALREADY present, this entry was integrated before —
   do NOT overwrite it. The original is already safe; leave it untouched.

Use this shape:

   ---
   ...other frontmatter...
   manually_added: true
   integration_status: integrated
   original_body: |
     <my original words, exactly as I typed them>
   ---
   <the cleaned-up / integrated body goes here>

------------------------------------------------------------------------------
THEN INTEGRATE IT INTO THE GRAPH
------------------------------------------------------------------------------
4. Improve the prose where it genuinely helps — clarity, structure, fixing
   obvious typos. Keep my voice. NEVER invent facts, events, names, dates, or
   feelings I didn't write. If something is ambiguous, leave it as I wrote it
   rather than guessing. The original_body you just saved is the safety net, not
   a license to rewrite freely.

5. Wire the entry into the knowledge graph with [[wikilinks]]. Read the body and
   identify every entity it touches — people, organizations, projects, topics,
   key elements, goals, habits, documents — and link each one with [[wikilinks]]
   in the body. For each entity:
     - SEARCH the scaffold for an existing note FIRST (exact and near-name match).
       If it exists, link to it. Never create a duplicate node.
     - If it genuinely doesn't exist and it's worth retrieving later, create a
       minimal stub from the matching template (in myPKA: Team Knowledge/
       Templates/) in the correct folder per the conventions, then link to it.
     - When you can't tell whether two names are the same entity, link to the
       existing one and note the possible duplicate at the end of your reply
       rather than forking the graph.
   Also fill in the structured frontmatter that has a clear home (date,
   category, mood, the linked_* arrays, key_element, tags) per the schema —
   structured data in frontmatter, narrative in the body. Don't invent fields
   the schema doesn't define.

6. Set the integration markers in frontmatter:
     - `integration_status: integrated`
     - keep `manually_added: true` (it records that the entry came from the
       cockpit's manual-add flow)

------------------------------------------------------------------------------
STAY NON-DESTRUCTIVE — CONFIRM BEFORE BIG CHANGES
------------------------------------------------------------------------------
7. Small touch-ups (typos, light cleanup, adding links, creating obvious stubs)
   you can just do. But if integration means a SUBSTANTIAL rewrite, a
   restructure, reclassifying the entry, or creating several new entity notes,
   SHOW me the plan and the proposed diff and wait for my OK before writing.
   When in doubt, ask. You only ever ADD the preserved original and the
   integrated body — you never delete my source text.

------------------------------------------------------------------------------
REFRESH THE COCKPIT
------------------------------------------------------------------------------
8. When the markdown is saved and correct, regenerate the SQLite mirror so the
   cockpit reflects the change. From {{ROOT}}, run the cockpit's regen script —
   in the standard layout that is:

     python3 "Expansions/mypka-cockpit/scripts/regen-mypka-db.py"

   If the cockpit lives elsewhere, run the `scripts/regen-mypka-db.py` inside the
   cockpit folder. The regen is non-destructive (markdown stays canonical; the DB
   is just the derived mirror). If Python or PyYAML is missing, say so and skip
   it — the existing mypka.db still serves until the next launch.

------------------------------------------------------------------------------
WHEN YOU'RE DONE
------------------------------------------------------------------------------
Tell me, in a few lines: that the original was preserved, what you linked and
what (if anything) you created, what you cleaned up, that integration_status is
now `integrated`, and whether the regen ran. Flag any possible duplicates or
anything you weren't sure about.
